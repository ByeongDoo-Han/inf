spring:
  application:
    name: inf
  datasource:
    url: jdbc:postgresql://localhost:5432/inf
    username: byeongdoohan
    password: p@ssw0rd
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # ?? ??: update, ?? ??: validate ?? none ??
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect